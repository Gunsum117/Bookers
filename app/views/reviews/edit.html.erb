<h1>Editing Book</h1>

<%= form_for(@review) do |f| %>
    <h4>タイトル</h4>
    <%= f.text_field :title %>

    <h4>本文</h4>
    <%= f.text_area :body %>
    <p>
    <%= f.submit 'Update Book' %>
    </p>

<% end %>

<%= link_to "Show", "/reviews/#{@review.id}", id: "mover5"  %>
<a class="back" href="/reviews"> Back</a>